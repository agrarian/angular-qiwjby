export class UserLookup {
    USER_ID: number;
    USER_DESC: string;
    constructor(
        USER_ID: number,
        USER_DESC: string
    ) {
        this.USER_ID = USER_ID;
        this.USER_DESC = USER_DESC;
    }
}
